import React from "react";

// Simple placeholder for particle overlay; can be extended to use tsParticles or a canvas library
const ParticlesEffect = ({ className = "" }) => (
  <div className={`particles-effect ${className}`} />
);

export default ParticlesEffect;
